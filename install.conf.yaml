- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim: nvim
    ~/.alias: alias

- shell:
  - git submodule sync --recursive
  - git submodule update --init --recursive
