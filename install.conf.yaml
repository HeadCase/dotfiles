- defaults:
    link:
      create: true
      relink: true

- clean: ['~','~/.config']

- link:
    ~/.ssh/config: ssh/config

- shell:
  - git submodule sync --recursive
  - git submodule update --init --recursive
